package File.Create;

public class Intro {
    /**
     * 创建文件对象相关构造器和方法
     * 相关方法
     * new File(String pathname) 根据路径构建一个File对象 --------------------------------直接指定路径创建文件
     * new File(File parent,String child) 根据父目录文件+子路径构建 ----------------------根据已知文件名，创建文件在已知文件目录上
     * new File(String parent,String child) 根据父目录+子路径构建 -----------------------通过路径，通过已知旧文件路径创建
     *
     * createNewFile 创建新文件
     *
     * Serializable Comparable
     *         File
     */
}
